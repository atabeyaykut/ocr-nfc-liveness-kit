# Azure Artifacts Registry Configuration
# Requires PAT (Personal Access Token) authentication

# Registry for @turkiye scope
@turkiye:registry=https://pkgs.dev.azure.com/turkiye/_packaging/kimlik-sdk/npm/registry/

# Always authenticate for @turkiye scope
always-auth=true

# Authentication token (will be injected by CI/CD)
; //pkgs.dev.azure.com/turkiye/_packaging/kimlik-sdk/npm/registry/:_authToken=${AZURE_ARTIFACTS_PAT}
; //pkgs.dev.azure.com/turkiye/_packaging/kimlik-sdk/npm/:_authToken=${AZURE_ARTIFACTS_PAT}

# Enable strict SSL
strict-ssl=true

# Log level
loglevel=warn
